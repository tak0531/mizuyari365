<div class="relative">
    <%= image_tag 'top.webp', class: 'w-full h-auto object-cover blur-top-bottom opacity-0', id:'fadeInImage' %>
   <div class="absolute top-0 left-0 right-0 bottom-0 flex flex-col justify-center items-center text-center text-gray-600"></div>
   <div class="container mx-auto mt-44">
    <h1 class="text-3xl font-bold text-green-700 mb-6 text-center border-b border-solid">水やりKeeper</h1>
    <p class="text-gray-600 mb-6 italic text-center">水やりKeeperは、観葉植物の水やりタイミングを管理するためのアプリです。</p>
    <p class="text-gray-600 mb-6 italic text-center">サービスをうまく活用して、植物を枯らさないようにしましょう！</p>
   </div>
   <div class="container mx-auto">
    <h1 class="text-3xl font-bold text-green-700 mb-6 text-center mt-44 border-b border-solid">水やりKeeperでできること</h1>
    <div class="container mx-auto mb-12 flex justify-center overflow-x-auto space-x-4 text-center">
      <div class="bg-white shadow-md rounded-lg overflow-hidden w-96 h-96 mb-12 flex-none">
        <div class="p-4">
          <h2 class="text-2xl font-semibold mt-6 mb-2">適切な水やり周期をMyページで通知</h2>
          <%= image_tag 'top_push.png',class:'mt-6' %>
        </div>
      </div>
      <div class="bg-white shadow-md rounded-lg overflow-hidden w-96 h-96 mb-12 flex-none">
        <div class="p-4">
          <h2 class="text-2xl mt-6 font-semibold mb-2">LINE連携すれば、水やり日にメッセージが</h2>
          <%= image_tag 'top_line.jpg', class:'mt-6' %>
        </div>
      </div>
    </div>
   </div>
</div>

<script>
  document.addEventListener("DOMContentLoaded", function() {
  function fadeInImage() {
    const image = document.getElementById("fadeInImage");
    if (image) { // 要素が見つかった場合のみ実行
      let opacity = 0;
      const intervalId = setInterval(function() {
        opacity += 0.01;
        image.style.opacity = opacity;
        if (opacity >= 1) {
          clearInterval(intervalId);
        }
      }, 20);
    }
  }
  fadeInImage();
});
</script>
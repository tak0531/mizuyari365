<div class="mx-auto flex flex-col">
    <div class="flex mx-auto items-center">
        <h1 class="text-3xl mt-4">検索結果</h1>
    </div>

    <div class="container mx-auto mt-2">
        <div class="flex flex-col grid grid-cols-3 gap-4 md:grid-cols-3 lg:grid-cols-3 row-span-5 mx-4 mb-12">
            <% @results.each do |result| %>
                <%= render 'products/search_result', result: result %>
            <% end %>
        </div>
    </div>
</div>